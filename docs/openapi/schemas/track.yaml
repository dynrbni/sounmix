components:
  schemas:
    Track:
      type: object
      properties:
        id: { type: string }
        title: { type: string }
        artist: { type: string }
        album: { type: string }
        isrc: { type: string }
